[[0], ["List[float]]:\n    \"", "Fetch historical candlestick data from Binance API.\"", "\n    import requests\n    import time\n    \n    if end_time == 0:\n        end_time = int(time.time())\n    \n    all_candles = []\n    current_time = start_time\n    \n    while current_time < end_time:\n        try:\n            url = f\"{BINANCE_API_BASE}/api/v3/klines\"\n            params = {\n                \"symbol", "symbol,\n                \"interval", "interval,\n                \"startTime", "current_time,\n                \"endTime", "end_time,\n                \"limit", 1000], {"seconds...": "file=sys.stderr)\n                time.sleep(retry_after)\n                continue\n            elif response.status_code != 200:\n                print(f", "candles": {"print(f": "ymbol: {args.symbol", "candles": "break\n            \n            all_candles.extend(candles)\n            \n            # Move to next batch\n            current_time = candles[-1][0] + 1\n            \n            # Small delay between requests\n            time.sleep(0.2)\n            \n        except requests.exceptions.RequestException as e:\n            print(f", "failed": {"e}": "file=sys.stderr)\n            \n            if retries > 0:\n                print(f", "left": {"retries})": "file=sys.stderr)\n                time.sleep(2)\n                retries -= 1\n                continue\n            else:\n                sys.exit(1)\n    \n    # Trim candles to exact time range\n    all_candles = [c for c in all_candles if start_time <= c[0] <= end_time]\n    all_candles.sort(key=lambda x: x[0])\n    \n    print(f", "symbol}": "return all_candles\n\n\ndef calculate_indicators(\n    candle: List[float]", "historical_candles": "List[List[float]]", "tick_prices": "List[float]\n) -> AnalyzeParams:", "Calculate technical indicators for a single candle.\"": "current_price = candle[4]\n    window_open = candle[0]\n    \n    # EMA 9 and 21 calculation\n    ema_9 = None\n    ema_21 = None\n    \n    if len(historical_candles) >= 21:\n        prices_21 = [c[4] for c in historical_candles[:21]]\n        ema_21 = prices_21[-1] if len(prices_21) > 0 else None\n        \n        if ema_21 is not None and len(historical_candles) >= 21:\n            ema_multiplier = 2 / (21 + 1)\n            ema_prev = ema_21\n            ema_21 = (prices_21[-1] * ema_multiplier) + (ema_prev * (1 - ema_multiplier))\n        \n        if len(historical_candles) >= 10:\n            prices_9 = [c[4] for c in historical_candles[:10]]\n            ema_9_multiplier = 2 / (9 + 1)\n            ema_9_prev = prices_9[-1]\n            ema_9 = (prices_9[-1] * ema_9_multiplier) + (ema_9_prev * (1 - ema_9_multiplier))\n    \n    # RSI 14 calculation\n    rsi = None\n    if len(historical_candles) >= 14:\n        closes = [c[4] for c in historical_candles[:14]]\n        gains = []\n        losses = []\n        \n        for i in range(1", "len(closes))": "change = closes[i] - closes[i - 1]\n            if change > 0:\n                gains.append(change)\n                losses.append(0)\n            else:\n                gains.append(0)\n                losses.append(abs(change))\n        \n        avg_gain = sum(gains) / 14\n        avg_loss = sum(losses) / 14\n        \n        if avg_loss == 0:\n            rsi = 100\n        else:\n            rs = avg_gain / avg_loss\n            rsi = 100 - (100 / (1 + rs))\n    \n    # Volume\n    volume_3 = historical_candles[-3][5] if len(historical_candles) >= 3 else None\n    volume_6 = historical_candles[-6][5] if len(historical_candles) >= 6 else None\n    \n    return AnalyzeParams(\n        current_price=current_price", "symbol": "str", "interval": "str", "start_time": "int", "end_time": "int", "mode": "str", "confidence_threshold": "float", "max_trades": "int = 100\n) -> Dict:", "Run backtest for a single configuration.\"": "print(f", ")\n    \n    candles = fetch_binance_candles(symbol, interval, start_time, end_time)\n    \n    if len(candles) == 0:\n        print(\"No candles\")\n        return None\n    \n    results = []\n    signals_list = []\n    tick_prices = []\n    \n    # Configuration parameters\n    mode_params = {\n        \"safe": {"bet_size_pct": 0.25, "min_confidence": 0.3}, "aggressive": {"bet_size_pct": 1.0, "min_confidence": 0.2}, "degen": {"bet_size_pct": 1.0, "min_confidence": 0.0}}, "enumerate(candles)": "if total_trades >= max_trades:\n            break\n        \n        timestamp = candle[0]\n        current_price = candle[4]\n        \n        # Track 2-second ticks\n        tick_prices.append(current_price)\n        if len(tick_prices) > 100:\n            tick_prices = tick_prices[-100:]\n        \n        # Calculate indicators\n        params = calculate_indicators(\n            candle", "candles[": "i]", "window_open": "actual_outcome =", "UP": "lif current_price < window_open:\n            actual_outcome =", "DOWN": "lse:\n            actual_outcome =", "mode_params[\"min_confidence": "total_trades += 1\n            price = calculate_token_price(confidence)\n            bet_size = mode_params[", "bet_size_pct": "bankroll\n            \n            # Calculate outcome\n            win_amount = 0\n            if signal.direction == actual_outcome:\n                wins += 1\n                win_amount = bet_size * price\n            \n            bankroll += win_amount\n            \n            signals_list.append({", "config": "mode", "confidence_threshold": "confidence_threshold", "datetime": "datetime.fromtimestamp(timestamp)", "window_ts": "window_timestamp", "signal": "signal.direction", "confidence": "round(confidence", "actual_outcome": "actual_outcome", "win_amount": "win_amount", "new_bankroll": "round(bankroll", "mode": "mode", "total_trades": "total_trades", "wins": "wins", "win_rate": "wins / total_trades if total_trades > 0 else 0.0", "final_bankroll": "round(bankroll", "total_return": "round(bankroll - initial_bankroll", "return_pct": "round((bankroll - initial_bankroll) / initial_bankroll * 100", "signals": "signals_list"}, "main()": "", "point.\"": "parser = argparse.ArgumentParser(description="}, "print(f": "nterval: {args.interval"}, {"print(f": "ax trades per config: {args.max_trades"}, [0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7], ["safe", "aggressive", "degen"], [], {"Conf": {"\n                      f": "rades: {config_result['total_trades']"}, "Wins": {"\n                      f": "eturn: {config_result['return_pct']"}, ")\n    \n    print()\n    print(\"=\" * 70)\n    print(\"Excel Output\")\n    print(\"=": 70, "Summary\"\n    ws_summary.append([\"Mode\", \"Confidence\", \"Trades\", \"Wins\", \"Win Rate\", \"Final Bankroll\", \"Return %": "for r in results:\n        ws_summary.append([\n            r[", "f": "r['return_pct']"}, [1], ["Config", "Confidence", "Datetime", "Window TS", "Signal", "Actual", "Win Amount", "New Bankroll"], [0], ["signals"], ["sig[\"config"], ["confidence_threshold"], ["datetime"], ["window_ts"], ["signal"], ["actual_outcome"], ["win_amount"], ["new_bankroll"], ["A"], ["B"], ["H"], ["Datetime", "Config", "Bankroll"], [0], ["signals"], {"r[\"signals\"][0][\"datetime": "ws_bankroll.append([\n                    sig[", "print(f": "Results saved to: {args.output"}]